package class7;

public class ForWhileLoop3 {
    public static void main(String[] args) {

        for (int i = 0; i < 10; i++) {
            System.out.println(i);

        }
        //When you know exactly how many times you want to loop
        // through a block of code, use the for loop instead of a
        // while loop.
        // when we do not know how many times we need
        // to loop(repeat) a block of codes, so we need to use while loop.
        // most of the time it happens when the user input is involved.
        // no boolean with for loop only boolean with while loop

        // initialize
        /* 1st int i=0; prints
        2nd i<10; the condition prints
        3rd i++; increment prints
        4rd i<10; if condition true
        5th System.out.println(number); body prints
         */

        }
}
