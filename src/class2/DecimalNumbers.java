package class2;

public class DecimalNumbers {
    public static void main(String[] args) {
        // if we want to store decimal numbers (10.2, 110.2, 1.6 ) in to computer memory using Java we can use following types of boxes.
        // Float and Decimal
        float myNumberBox=10.5f;
        double largeDecimalBox=134545455.12121;
        System.out.println(myNumberBox);
        // if we want to store charters (#,$,o,s,4) we can use char datatype.

        char letter= 's';
        char gender='M';
        char symbol= '#';
        char numberChar='1';
        // String dta type belongs to a different category called non-primitives
        // that's why for String rules are a little different
        // for example S of String is upper case its color is also not blue we will discuss these details later on.

        String name="sam";


        boolean hungry=true; // ture means yes
        boolean sleepy=false; // false means no
        // we can not put yes or no directly it supposed to be true and false
        System.out.println(sleepy);

    }
}
