package class11;

import java.util.Scanner;

public class DogTester {
    public static void main(String[] args) {
    // for example: creating an actual object from the class Scanner
       // Scanner scanner=new Scanner(System.in); Scanner class, scanner is object,
       // scanner.next(); .next() is methode which avoid repeating the codes everytime
    // This is how we create the objects of a class
        Dog dog1= new Dog();
        dog1.bark();
        dog1.sleep();
    }

}
